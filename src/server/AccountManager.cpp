#include "AccountManager.h"

// TODO:
// Load account info into memory.
bool InilializeAccountManager(const char* path)
{
    return true;
}

// TODO: Only need to lock when trying to write data, no lock is needed when reading contents.
